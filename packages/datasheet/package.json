{
  "name": "@bajeiros/datasheet",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Catálogo da ficha do protótipo (DF-21) — seções, campos tipados, faixas, sugestões do modelo 3D e progresso, puro TypeScript, sem IO",
  "exports": {
    "./*": "./src/*.ts"
  },
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@bajeiros/core": "*"
  },
  "devDependencies": {
    "typescript": "^5.4.5",
    "vitest": "^4.1.11"
  }
}
