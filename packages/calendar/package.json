{
  "name": "@bajeiros/calendar",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Calendário de competições (DF-33) — ciclo da temporada, contagem regressiva, recorte pessoal, iCalendar e o parser da tabela oficial de prazos; puro TypeScript, roda em browser e Node",
  "exports": {
    "./*": "./src/*.ts"
  },
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.4.5",
    "vitest": "^5.0.0"
  }
}
