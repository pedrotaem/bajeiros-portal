{
  "name": "@bajeiros/auth",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Cliente OIDC code flow + PKCE do Cognito Managed Login — único lugar do monorepo que conhece Cognito (revisão v2, R-Seg)",
  "exports": {
    "./*": "./src/*.ts"
  },
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.4.5",
    "vitest": "^4.1.11"
  }
}
